<div class="row">
  <div class="col-md-3">
    <div class="box box-primary">
      <div class="box-body box-profile">
        <h3 class="profile-username text-center"><%= @member.team.name %></h3>
        <ul class="list-group list-group-unbordered">
          <li class="list-group-item">
            <b>提出者</b> <span class="pull-right"><%= @member.user.name %></span>
          </li>
          <li class="list-group-item">
            <b>希望日</b> <span class="pull-right"><%= @shift_submission.submitted_date_format %></span>
          </li>
          <li class="list-group-item">
            <b>希望時間</b> <span class="pull-right"><%= @shift_submission.submitted_time %></span>
          </li>
        </ul>
        <% unless @shift_adjustment %>
          <%= link_to '希望日時変更', edit_users_shift_submission_path(@shift_submission), class: "btn btn-success btn-block" %>
        <% end %>
        <hr>
        <%= link_to 'シフトカレンダー', users_member_path(@member), class: 'btn btn-default btn-block' %>
      </div>
    </div>
  </div>
  <div class="col-md-9">
    <% if @shift_adjustment %>
      <div class="box">
        <div class="box-header">
          <h3 class="box-title">シフト調整済</h3>
          <div class="box-tools">
          </div>
        </div>
        <div class="box-body no-padding">
          <table class="table">
            <tbody>
              <tr>
                <th>調整者</th>
                <th>勤務日</th>
                <th>開始時間</th>
                <th>終了時間</th>
              </tr>
              <tr>
                <td><%= @shift_adjustment.account_name %></td>
                <td><%= @shift_submission.submitted_date_format %></td>
                <td><%= @shift_adjustment.start_time %></td>
                <td><%= @shift_adjustment.end_time %></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    <% else %>
      <div class="callout callout-danger">
        <p>シフト希望が承認されていません</p>
      </div>
    <% end %>
  </div>
</div>
